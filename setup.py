from setuptools import find_packages, setup

setup(
    name='chem_dataset',
    packages=find_packages(),
    version='0.1.0',
    description='Parsing, standartizing, and filtering 2d molecules data',
    author='Almaz Tagir',
    license='MIT',
    setup_requires=['wheel', 'pip'],
)
